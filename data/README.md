# Data

The repo has two main data paths:

- `sample_prices.csv` for the structured benchmark
- `real_us_largecap_panel.csv` for the public large-cap extension

The structured benchmark is generated by `src/sample_data.py`. It is there
because the original Georgia Tech project used a confidential university
database that cannot be redistributed here.
