# Spectral Signal Extraction for Statistical Arbitrage

I built this project around a Georgia Tech research project on residual
mean reversion in equities. The main question was whether a cleaner
residualization step leads to a better stat-arb signal.

More specifically: if I remove common market and sector structure with
Marchenko-Pastur filtering instead of a fixed-rank PCA shortcut, do the
remaining residuals behave better in a dollar-neutral, beta-controlled,
cost-aware long/short strategy?

That is what this repo tests.

The original historical panel came from a confidential university database, so
I cannot post it here. That is why this repo uses a structured benchmark panel
with the same basic workflow. I also assembled the GitHub repo later from my
private research archive, which is why some summaries and evidence notes have
newer commit dates than the original project period.

## The Idea

In a cross-sectional stat-arb strategy, the signal often comes from residual
mean reversion. The whole project is built around one question: if I change the
way I remove common structure from returns, do I get a better downstream
signal?

The repo contains the residualization code, the PCA versus RMT comparison, the
long/short portfolio construction logic, the cost and regime diagnostics, and a
real-stock extension on a large-cap panel.

The core sequence is:

1. remove broad common structure from stock returns
2. look at what is left over
3. ask whether unusually large residual moves tend to reverse

The hard part is step 1. If the covariance estimate is noisy, then the
residuals can be poor. This project compares two ways of doing that step:

- raw fixed-rank PCA
- Marchenko-Pastur-filtered PCA

Everything after that is kept as similar as possible so the comparison stays
fair.

## Data

The repo includes a benchmark panel generated by
[src/sample_data.py](src/sample_data.py). It has:

- `24` synthetic equities
- `6` sectors
- business dates from `2019-01-02` to `2025-06-30`
- one market factor
- one style factor
- one sector factor per sector
- residual mean-reversion structure

That benchmark is there because the original historical panel cannot be
redistributed.

The repo also includes a separate real-data extension on a public large-cap
panel built from Nasdaq history:

- [data/real_us_largecap_panel.csv](data/real_us_largecap_panel.csv)
- [reports/real_largecap_run_summary.md](reports/real_largecap_run_summary.md)
- [results/real_us_largecap](results/real_us_largecap)

## How I Tested It

The workflow is:

1. take a rolling return window
2. estimate the correlation structure
3. remove common directions with either raw PCA or MP-filtered PCA
4. compute residual z-scores
5. long the most negative residual names and short the most positive ones
6. enforce dollar neutrality, beta control, position limits, and transaction
   costs
7. apply weights one day later to avoid look-ahead

So the project is not just a covariance exercise. It carries the residuals all
the way through to a real backtest.

## Included Benchmark Split

The benchmark in this repo uses:

| Split | Dates | Purpose |
|---|---:|---|
| Train | 2019-01-02 to 2021-12-31 | first-pass design |
| Validation | 2022-01-03 to 2022-12-30 | choose the final spectral sleeve |
| Holdout | 2023-01-02 to 2025-06-30 | final out-of-sample evaluation |

The original Georgia Tech project was closer to a rolling walk-forward study,
but this split makes the benchmark easier to inspect.

## Results

The main comparison on the benchmark is:

| Strategy | Validation Sharpe | Holdout Sharpe | Holdout Max DD | Holdout Beta |
|---|---:|---:|---:|---:|
| Raw PCA Residual | `-0.87` | `-0.21` | `-5.1%` | `0.011` |
| RMT-Filtered Residual | `1.04` | `1.73` | `-2.7%` | `-0.004` |

The message is simple. In this benchmark, the RMT-filtered residuals behave
better than the raw PCA residuals under the same portfolio rules.

That does not mean the strategy is ready for production. It means the
residualization step matters.

## Cost Sensitivity

The strategy is sensitive to cost, which is exactly the sort of thing a stat
arb project should show honestly.

- `1` bp cost: holdout Sharpe `5.63`
- `5` bps cost: holdout Sharpe `1.73`
- `10` bps cost: holdout Sharpe `-3.36`

So the signal looks much weaker once trading frictions get large. That is an
important limitation, not something to hide.

## Notes On The Original Run

The original Georgia Tech project is summarized separately in:

- [reports/original_project_summary.md](reports/original_project_summary.md)
- [reports/original_project_evidence/original_historical_run_summary.md](reports/original_project_evidence/original_historical_run_summary.md)
- [reports/original_project_evidence/original_results_table.md](reports/original_project_evidence/original_results_table.md)
- [results/original_historical_performance_summary.csv](results/original_historical_performance_summary.csv)

The saved summary of that original run is:

- raw PCA Sharpe around `0.8`
- RMT-filtered Sharpe around `1.3`
- max drawdown around `8%`
- `|beta_SPY| < 0.05`

So the repo does two jobs at once: it gives a runnable version of the method
and it preserves a summary of the original historical run.

## Real Large-Cap Extension

The repo also includes a real-stock extension on a public large-cap panel. That
run uses a slower profile with lower turnover and benchmark gating.

Headline result for the selected real-data sleeve:

- validation Sharpe `0.42`
- holdout Sharpe `0.54`
- holdout max drawdown `-3.8%`
- holdout average turnover `0.022`

That extension is useful because it shows the same idea on actual stocks,
separately from the synthetic benchmark.

## Run The Project

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -e ".[dev]"
python -m src.sample_data
python -m src.run_backtest
python -m src.build_reports
pytest
```

To run the real large-cap extension:

```bash
python -m src.build_reports \
  --input data/real_us_largecap_panel.csv \
  --results-dir results/real_us_largecap \
  --figures-dir figures/real_us_largecap \
  --strategy-profile real_largecap
```

What matters most here is the comparison itself. The repo keeps the residual
construction, portfolio rules, and costs explicit, then changes the
residualization step and checks what happens. That makes it a useful research
record even though it is not a live trading claim.
